@model IEnumerable<OnlineShop.ViewModel.EmployeeViews.EmployeeDisplayViewModel>
@*@model OnlineShop.ViewModel.EmployeeViews.EmployeeDisplayViewModel*@

<h1>Employees</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>

@*@foreach (var item in Model) {*@
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
                @*@Html.DisplayNameFor(model => item.Name)*@
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Age)
                @*@Html.DisplayNameFor(model => item.Age)*@
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Position)
                @*@Html.DisplayNameFor(model => item.Position)*@
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                    @*@Model.Name*@
                    @*@item.Name*@
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Age)
                    @*@Model.Age*@
                    @*@item.Age*@
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Position)
                    @*@Model.Position*@
                    @*@item.Position*@
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>

                    @*<a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@Model.Id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@Model.Id">Delete</a>*@
                </td>
            </tr>
        }
    </tbody>
</table>
@*}*@
